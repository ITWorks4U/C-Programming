SOURCES = 42_inheritance.c 42_main.c
HEADER = 42_inheritance.h
FLAGS = -g3 -Wall
OUTPUT = application

all:
				@gcc $(FLAGS) $(SOURCES) $(HEADER) -o $(OUTPUT)
				$(info App "$(OUTPUT)" built.)

clean:
				@rm $(OUTPUT)
				$(info App "$(OUTPUT)" removed.)